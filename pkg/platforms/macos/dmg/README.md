It's a docker image based on [create-dmg](https://github.com/sporsh/docker-create-dmg) with
a possibility of adding "Applications" symlink to a dmg image so that user can drag-and-drop the app to the 
Applications folder.